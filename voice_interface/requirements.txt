# Prime Spark Voice Command Hub - Requirements

# FastAPI framework
fastapi==0.104.1
uvicorn[standard]==0.24.0
python-multipart==0.0.6

# HTTP client
requests==2.31.0

# Redis cache
redis==5.0.1

# Data validation
pydantic==2.5.0
pydantic-settings==2.1.0

# WebSocket support
websockets==12.0

# Audio processing
pyaudio==0.2.14
wave

# Speech-to-text (Whisper)
# Use faster-whisper for Pi 5 optimization
faster-whisper==0.10.0
# OR use openai-whisper (heavier):
# openai-whisper==20231117

# Text-to-speech
# Piper-TTS (install separately: pip install piper-tts)
# Fallback: espeak

# NLU/LLM
# Uses Ollama via API (no install needed)

# Utilities
numpy==1.24.3
python-dotenv==1.0.0
python-dateutil==2.8.2
